# To-Do-List---Python
A simple command-line To-Do List application built with Python. It allows users to add, view, and remove tasks through a menu interface. Tasks can be saved to and loaded from a text file, so the list persists between sessions. The project demonstrates basic Python concepts like lists, loops, functions, and user input.
